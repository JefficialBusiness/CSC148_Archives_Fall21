# Compute the area of a circle given it's radius

import math
r = float(input("Enter radius: "))

area = math.pi*(r**2)  # Performs pi*r^2 operation per formula for finding circle area

print("The area is: ", str(area))
